## 协议类型

有两类协议：
- 框架层内部协议
- 逻辑层用户协议

go-xserver 力求不限制用户的协议规范，即比如：
- Login 可以自定义协议，不管是使用 TCP 、 HTTP ； 还是使用 struct 协议、 proto 协议 等
- Gateway 也是一样
- 后续其他服务器同样力求不限制用户的协议规范

## 服务器默认实现

常见的逻辑服务器，都会提供一个默认实现。以供参考、或者直接使用
